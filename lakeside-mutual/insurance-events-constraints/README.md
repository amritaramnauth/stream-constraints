# Insurance Events Constraints

This project demonstrates deduplication constraint with Lakeside Mutual InsuranceQuoteRequestEvents.

### Usage
You can compile code with `sbt compile`, and run it with `sbt run`.
